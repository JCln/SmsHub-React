import * as Types from '../constants/ActionTypes';
import * as ENRoutes from '../constants/ENRoutes';

export const incrementValue = () => ({ type: Types.Increment });
export const LoginValue = () => ENRoutes.Login;